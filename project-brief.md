# 프로젝트 브리핑 — studyHard 파일럿 랜딩 페이지

Claude Code에게: 아래 내용을 읽고 첫 번째 작업(랜딩 페이지 코드 생성)을 진행해줘.
불명확한 부분이 있으면 코드부터 짜지 말고 먼저 질문해줘.

---

## 1. 배경 및 목표

- 개발자(대학생, 4학년, 컴퓨터공학 전공)가 **AWS EC2(Ubuntu, t3.micro, 프리티어)** 서버 운영을
  연습하기 위해 진행 중인 개인 사이드 프로젝트.
- 이미 완료한 것:
  - EC2 인스턴스 생성, SSH 접속(키 기반) 성공
  - 기본 서버 세팅 1단계 완료: 시스템 업데이트, sudo 유저 생성, 비밀번호 로그인 차단,
    ufw 방화벽 설정(22번 SSH, 80번 HTTP만 허용)
  - Nginx 설치 및 정상 구동 확인
  - Node.js, Java(OpenJDK 17), Python3/pip/venv 설치 완료 (Next.js / Spring Boot / FastAPI 배포 대비)
  - 보안 그룹: SSH(22)는 본인 IP만 허용, HTTP(80)는 0.0.0.0/0 허용
- 최종 목표 서비스: "공부 기록 작성 + AI 기반 요약/문제 생성" 웹앱 (스터디 노트 앱)
  - 향후 계획: Tiptap 기반 리치 텍스트 에디터(폰트 크기/굵기, 이미지 삽입), 페이지 PDF export →
    AI API로 Markdown 요약 → AI API로 문제 자동 생성 파이프라인. S3(파일) + MySQL(메타데이터) 연동 예정.
  - **단, 이번 파일럿 작업 범위는 "로그인 전 첫 화면(랜딩 페이지)"만 해당하며, 아래 명세는
    전부 정적 Mock 데이터 기반 데모 페이지임. 백엔드/DB/인증 로직은 이번 범위에 포함하지 않음.**

## 2. 이번 작업 범위

- **프론트엔드 랜딩 페이지 1개**를 만들어서, 완성 후 위 EC2 Ubuntu 서버에 Nginx 리버스 프록시로
  올려서 실제 접속 테스트를 해보는 것이 목표.
- 스택 제안: Next.js (기존 개발 경험 있음). 별도 지정 없으면 Next.js + Tailwind CSS로 진행.
- 스크롤 시 섹션이 순차적으로 나타나는 원페이지(스크롤 인터랙션) 구조.
- 모든 데이터(그래프 수치, 캘린더 히트맵 강도, 예시 제목 등)는 **하드코딩 또는 랜덤값**으로 채울 것.
  실제 로그인, 실제 저장, 실제 API 연동 없음.

## 3. 화면 명세

### 첫 페이지 (히어로 섹션)
- 상단: 로고 텍스트 "studyHard"
- 상단 아래 좌측: ID / PW 입력 필드 2개 (로그인 폼 UI만, 실제 인증 로직 없음 — 버튼 클릭 시
  아무 동작 없거나 콘솔 로그만 찍어도 됨)
- 하단: 랜덤 명언 표시 영역 (명언 목록을 배열로 하드코딩해두고 랜덤 1개 표시)

### 스크롤 섹션 1 — "공부 기록을 작성하세요."
- 상단: 위 타이틀 텍스트
- 중단: 정적 텍스트/이미지로 만든 노트 에디터 미리보기
  - 예시 문구: "오늘 하루 공부한 것을 자유롭게 작성하세요. 그리기, 사진 첨부 등 모두 가능합니다."
  - 실제 타이핑 가능한 에디터 아님. 정적 미리보기(스크린샷 느낌의 UI 카드)면 충분.

### 스크롤 섹션 2 — "공부 현황을 파악하세요."
- 상단: 위 타이틀 텍스트
- 중단 박스: "공부 기록 마무리" 미리보기 카드
  - 상단 필드: "오늘의 순공 시간은?" — 입력창 UI (동작 없음, 모양만)
  - 하단 필드: "공부한 종류는?" — 입력창 UI (동작 없음, 모양만)

### 스크롤 섹션 3 — "공부 현황 그래프"
- 상단: 위 타이틀 텍스트
- 중상단 우측: "주간" / "월간" 토글 버튼 2개
- 그래프 영역:
  - 주간 선택 시: 월~금(또는 일~토) 요일별 공부 시간 막대/선 그래프
  - 월간 선택 시: 주 단위(1주~4~5주)로 묶은 공부 시간 그래프
  - 데이터는 랜덤 값 생성 (매번 새로고침 시 달라져도 무방)
  - 그래프 라이브러리는 Recharts 권장 (Next.js/React와 궁합 좋음)

### 스크롤 섹션 4 — "캘린더"
- 상단: 위 타이틀 텍스트
- 중상단 우측: "n년 n월" 드롭다운 (월 선택 UI, 실제 월별 데이터 전환 없어도 됨 — 선택 시 그냥
  같은 랜덤 목업 재렌더링 정도로 충분)
- 본문: **GitHub 잔디(contribution graph) 스타일 히트맵 캘린더**
  - 날짜별로 "그날 작성한 공부 기록 텍스트 양"에 비례해 색상 진하기가 달라짐 (5단계 정도:
    없음/연함/보통/진함/매우 진함)
  - 데이터는 랜덤 값으로 생성
  - 구현 시 `react-calendar-heatmap` 라이브러리 사용 권장 (아니면 직접 CSS Grid로 구현해도 됨)

### 스크롤 섹션 5 — "오늘 공부를 점검하세요."
- 상단: 위 타이틀 텍스트
- 중단 박스 상단 문구: "공부한 날 내용을 골라 AI 요약과 문제를 생성하세요."
- 중단 박스 하단: 날짜 선택 드롭다운
  - 드롭다운에는 날짜 + 그날 저장된 공부 기록 제목이 함께 표시되어야 함
    (예: "9/15 — 자료구조 트리 정리", "9/16 — 알고리즘 DP 문제풀이")
  - 제목들은 하드코딩된 예시 3~5개 정도면 충분

## 4. 명시적으로 제외된 것 (하지 않아도 되는 것)
- 실제 로그인/인증/세션 처리
- "체험(비로그인 미리보기)" 버튼 — 기획에서 제외됨
- 실제 데이터베이스, API 연동, S3 연동
- 실제 타이핑 가능한 에디터 (섹션 1은 정적 미리보기만)

## 5. 배포 관련 참고 (프론트 코드 완성 후 진행할 것)
- 완성된 정적/Next.js 빌드 결과물을 EC2로 옮겨서 Nginx 리버스 프록시(80번 → Next.js 내부 포트)로
  서빙할 예정. 코드 생성 단계에서는 이 부분 신경 쓰지 않아도 되며, 로컬에서 정상 빌드/구동되는 것에
  집중하면 됨.

---

**Claude Code에게 요청**: 위 명세를 기반으로 Next.js + Tailwind CSS 프로젝트 구조를 잡고,
첫 페이지 및 5개 스크롤 섹션을 하나의 원페이지 컴포넌트로 구현해줘. 애매한 부분(예: 정확한 톤/색상,
폰트, 특정 인터랙션 디테일)은 임의로 정하되, 위 "제외된 것" 항목은 절대 구현하지 마.
